[project]
name = "pyledger-config"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = ["pydantic>=2.13.3", "pydantic-settings>=2.14.2"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
namespaces = true
